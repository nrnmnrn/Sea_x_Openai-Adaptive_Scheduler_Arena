# S03-A：A／B 內部開發紀錄

本紀錄是既有 [Ticket #6](https://github.com/nrnmnrn/Sea_x_Openai-Adaptive_Scheduler_Arena/issues/6) 的本地工作補充，不新增 Ticket 或修改契約。Parent：[SP-03 #3](https://github.com/nrnmnrn/Sea_x_Openai-Adaptive_Scheduler_Arena/issues/3)。使用者明確要求先保留本地紀錄，因此沒有更新 GitHub。

## 開工範圍

- 使用者於本次對話確認 A／B 開發提案；僅限 SP-03 v0.3-draft、workflow §2.1 與工作分類表的 S03-A 內部部分。
- 分支：`codex/s03-a-candidate-internals`。
- 基準：`e7e195209a3a0e7dddbf8e09b3d53120efd12477`；開工時已透過 `git ls-remote origin refs/heads/main` 確認與遠端 main 一致。
- 程式檔案集：`candidate_loop/`；測試檔案集：`tests/test_candidate_loop_*.py`；本地證據：本檔。
- A：Candidate ID、版本與父鏈管理、五版上限；既有欄位的 code、原因、feedback、結果呈現。
- B：單版候選內部流程組裝；外部 Planner、全敗輸入、共用 evaluator 集中留待接線，不定義其公開請求格式。
- 接口：I03、I04、I05、I07、I08；交接：H3～H6、H8；待決：D2～D9。

## 排除與未驗證

- 不呼叫外部 Planner、Evaluator、Critic 或 LLM，不以固定全敗／passed 或 Mock 證明上游可用。
- 不自行決定 baseline、null gate、timeout、retry、隔離或跨組 schema，不承接共用評估器責任。
- 不修改 Live Run、scheduler、共用 UI shell、Skill Library，不登錄／啟用策略。
- 不實作 S03-B 多版 Critic loop 或 S03-C 完整恢復；版本上限測試不構成迭代流程交付。
- 未有真實 H4 前，正式候選產生／評估入口維持不可用；組內 ledger／呈現測試只證明內部行為。
- S01-FULL、S02-A 真實版本、開發版試接、main 整合及完整 S03-A 驗收均待驗證。
- 不新增依賴；沿用 pyproject.toml 已列工具。既有未追蹤檔案不納入本工作。

## 驗證計畫

檢查 ID 重複、父鏈一致、五版與第六版拒絕、失敗不消耗版本、輸入／輸出修改不破壞內部紀錄、缺少評估不顯示通過，以及未接線流程不呼叫外部服務。只用內部測試資料，不把測試資料稱為真實上游成果。

完成程式後執行完整 `uv run pytest`、`uv run ruff check .`、`uv run ruff format --check .`、`git diff --check`，並獨立審查 Standards 與 Spec。若出現型別或 pre-commit 設定，再執行相應檢查。

## 已完成的內部行為與檔案

| 檔案 | 行為 |
| --- | --- |
| `candidate_loop/__init__.py` | 僅說明內部用途，不匯出跨組 API。 |
| `candidate_loop/lineage.py` | 接收呼叫端提供的內部紀錄；檢查非空／唯一 ID、連續版本、直接父版本與單一鏈五版上限；驗證與複製完成才寫入。 |
| `candidate_loop/presentation.py` | 準備組內呈現資料，保留 code、原因、feedback 與原始結果。缺結果標示 `unchecked`；提供結果標示 `unverified`；收到的 status 亦標示未驗證，不計算 gate。 |
| `candidate_loop/flow.py` | 組合紀錄與呈現；`run()` 明確拋出 `DisconnectedRuntimeError`，沒有外部 callback 或可以繞過前提的布林參數。 |
| `tests/test_candidate_loop_lineage.py` | 版本／父鏈、重複拒絕、五版邊界、複製失敗及防禦副本測試；包含子類別複製行為不能替換已驗證 ID 的回歸案例。 |
| `tests/test_candidate_loop_presentation.py` | 內容保留、缺失／收到的通過結果仍未驗證，以及未接線入口拒絕執行的測試。 |

本次呈現是供未來組內畫面使用的資料準備，沒有接入 Gradio tabs。內部紀錄僅沿用已列 Candidate 欄位，opaque payload 不定義跨組形狀；單一 ledger 的五版限制不代表跨 run／adaptation 的整合預算驗收。

## 驗證結果

環境：Python 3.11.16、pytest 9.1.1、Ruff 0.16.6。`uv sync` 使用原 pyproject.toml 成功建立環境；未新增依賴或修改共用設定。根目錄 `uv.lock` 由 uv 自動產生，未手動編輯。

| 命令／檢查 | 結果 |
| --- | --- |
| `git ls-remote origin refs/heads/main` | 成功；開工基準與遠端 main 相同。 |
| `uv sync` | 成功；首次因快取存取權限受阻，經工具授權後完成。 |
| `uv run pytest` | 最終完整配置的套件共 14 項，全部通過。 |
| `uv run ruff check .` | 通過。 |
| `uv run ruff format --check .` | 通過。 |
| `git diff --check` | 通過；另外對全部新增程式、測試及本檔做 no-index whitespace 檢查，無空白格式診斷。 |
| `uv build` | 失敗：setuptools 自動發現 `templates`、`candidate_loop`、`competition_info` 多個頂層套件，無法建置來源發行包。本次未改套件探索或其他共用設定。 |
| 型別檢查／pre-commit | 未找到設定，不新增工具或跳過既有檢查。 |
| Standards 獨立審查 | 最終 0 項待修正發現。 |
| Spec 獨立審查 | 複製行為及內部介面邊界問題已修正、補測試並複查；最終 0 項待修正發現。 |

Terra 負責實作與修正；Luna 執行最終完整檢查；兩個獨立 Terra 審查分別核對 Standards／Spec。審查以開工 main 為基準，將上述未提交新檔視為新增內容；沒有審查或納入使用者原有的未追蹤檔案。

## 狀態與後續

已完成本次核准 A／B 的內部里程碑；完整 S03-A 與 Parent 保持 Open。未 commit、push、建立遠端 PR、merge 或 closeout；依使用者要求只留本地證據。

仍需各 Provider 與 main 負責人釐清 I03～I05、I07、I08 對應決策，提供真實 H4 全敗與共用評估能力，再依 workflow 做受控試接、正式依賴驗證、完整切片驗收及合併授權。套件打包須另確認團隊目錄／套件探索設定後處理。本紀錄不代表正式整合或可供 SP-04 晉升的真實 passed 證據，不自動開始 S03-B／C。

## 2026-09-12：SP-01 merge compatibility check

- 已將 `origin/main` 的 SP-01 Scheduling Arena local demo 合併至本分支：merge commit `ac20dc8`，所含 main tip 為 `6cefc0c`；合併無衝突。
- 逐項比對 S03-A 的 A／B 範圍、backend contract、adaptation contract H3～H6／H8 與新 `SchedulerBackend`。`candidate_loop/` 沒有 import 或修改 scheduler；它仍只記錄／呈現未驗證 Candidate，`DisconnectedCandidateFlow.run()` 仍拒絕 Planner 或 evaluator 執行。因此不需要相容性程式修正，也不會把目前 main 的 local backend 誤報為 H4 真實全敗或已凍結的共用 evaluator。
- 完整本地驗證（不執行外部 API，並以 `PYTHONDONTWRITEBYTECODE=1` 避免重寫產生檔）：`uv run pytest -q --backend local`，49 passed；`uv run ruff check .`，通過；`uv run ruff format --check .`，49 files already formatted；`git diff --check`，通過。
- 型別檢查與 pre-commit：未發現 mypy／pyright 或 pre-commit 設定，未新增工具。`uv build` 仍失敗於 setuptools 的 flat-layout 自動探索（`artifacts`、`templates`、`experiments`、`candidate_loop`、`competition_info` 多個 top-level packages）；本次未改套件探索或共用 build 設定。
- 分支原先追蹤的 `__pycache__`、`.pyc` 與 `scheduler_arena.egg-info` 為產生檔；本次 PR 清理僅自 Git index 移除並新增 ignore 規則，保留工作目錄內既有檔案且不修改其內容。`uv.lock` 維持不變。
- 兩個獨立審查以 `origin/main` 的 `6cefc0c` 與 S03 程式 commits `6bf482c`／`f5fb921` 為基準，分別核對 Standards 與 Spec；兩者皆為 0 項 actionable findings。審查確認無相容性程式修正需要，並重申 H4、共用 evaluator、H5／H6、S02-A 與 D2～D9 仍非本次交付的已證明能力。

此更新只補合併、檢查與 readiness 證據。S03-A 仍是 prototype milestone：S01-FULL 已在 main 不等於 H3／H4／H5／H6 的提供者交付或完整切片核可；在真實 S02-A 全敗輸出與經核准的共用 evaluator／promotion evidence 出現前，不得接通或宣稱正式交付。
